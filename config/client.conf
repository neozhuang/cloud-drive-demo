[remote]
host = 127.0.0.1
port = 8888

[log]
log_level = info
log_file = logs/client.log

[storage]
download_dir = downloads

[transfer]
max_concurrent = 4
connect_timeout_ms = 5000
io_timeout_ms = 30000
